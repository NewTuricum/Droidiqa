plugins{
    //plugins with versions that need to be preloaded into the classpath of the library project without application
    alias("android-gradle") apply false
    alias("kotlin-gradle") apply false
    alias("dokka") apply false

}

